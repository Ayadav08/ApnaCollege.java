public class SortedArray {
    static boolean isSortedArray(int arr[], int index){
       if (index == arr.length-1){
        retrun true;

       }
       if(arr[index]>       )
    }
    public static void main(String[] args){
        int arr [] = {10,20,30,40,50};
        isSortedArray(arr, index: 0);
    }
}